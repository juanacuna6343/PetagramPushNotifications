{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch web-server",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/web-server/server.js",
            "cwd": "${workspaceFolder}/web-server",
            "console": "integratedTerminal",
            "env": {
                "NODE_ENV": "development"
            }
        }
    ]
}