{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start web-server",
            "type": "shell",
            "command": "npm install && npm start",
            "options": {
                "cwd": "${workspaceFolder}/web-server"
            },
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}